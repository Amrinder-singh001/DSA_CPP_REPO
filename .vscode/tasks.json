{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Run C++",
        "type": "shell",
        "command": "clang++ -std=c++17 ${file} -o ${fileDirname}/${fileBasenameNoExtension} && ${fileDirname}/${fileBasenameNoExtension}",
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }